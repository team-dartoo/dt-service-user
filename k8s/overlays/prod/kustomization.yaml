apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dartoo
resources:
  - ../../base
  - configmap.yaml
  - sealed-secret.yaml
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
      name: dt-user
