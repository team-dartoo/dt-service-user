spring:
  application:
    name: ${SPRING_APPLICATION_NAME:dt-service-user}
  lifecycle:
    timeout-per-shutdown-phase: ${SPRING_SHUTDOWN_TIMEOUT:30s}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    url: ${DB_URL}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    properties:
      dialect: ${HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
      format_sql: true
      hibernate.jdbc.time_zone: ${SPRING_JPA_HIBERNATE_TIME_ZONE:UTC}
  jackson:
    time-zone: ${SPRING_JACKSON_TIME_ZONE:UTC}
  mail:
    # JavaMailSender가 알아서 application.yml에서 긁어와서
    # SMTP relay 방식에서 사용
    host: smtp-relay.brevo.com
    port: 587
    username: ${BREVO_ACCOUNT_EMAIL}
    password: ${BREVO_SMTP_KEY}
    properties:
      mail:
        smtp:
          auth: true          # SMTP 서버에 username/password 인증 사용
          starttls:
            enable: true      # 평문 연결(587 포트)을 TLS 암호화로 업그레이드 (STARTTLS)
  security:
    oauth2:
      client:
        registration:
          naver:
            provider: naver
            client-id: ${OAUTH_NAVER_CLIENT_ID}
            client-secret: ${OAUTH_NAVER_CLIENT_SECRET}
            scope: [ name, email, gender, birthday, birthyear ]
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Naver
          kakao:
            provider: kakao
            client-id: ${OAUTH_KAKAO_CLIENT_ID}
            client-secret: ${OAUTH_KAKAO_CLIENT_SECRET}
            scope: [ profile_nickname, account_email ]
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Kakao
          google:
            provider: google
            client-id: ${OAUTH_GOOGLE_CLIENT_ID}
            client-secret: ${OAUTH_GOOGLE_CLIENT_SECRET}
            scope: [ profile, email, https://www.googleapis.com/auth/user.birthday.read, https://www.googleapis.com/auth/user.gender.read ]
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Google
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response # 계정을 식별할 때의 링크 응답의 1차 식별자
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://www.googleapis.com/oauth2/v4/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo

app:
  frontend-url: ${FRONTEND_URL:http://localhost:8080}  # 로컬에서는 백엔드와 같은 포트, 배포 시 환경변수로 프론트 도메인 지정
  jwt:
    issuer: "Dartoo"
    access-secret: ${JWT_ACCESS_SECRET}
    refresh-secret: ${JWT_REFRESH_SECRET}
    refresh-pepper: ${JWT_REFRESH_PEPPER}
    access-ttl-seconds: ${JWT_ACCESS_TTL_SECONDS:900} # 15분
    refresh-ttl-seconds: ${JWT_REFRESH_TTL_SECONDS:1209600} # 14일
  oauth:
    # 이 소셜 계정을 기존 DB의 어떤 이메일 계정에 붙여야 하는지
    # 결정할 때만 쓰는 짧은 수명의 링크 토큰용 키
    link-secret: ${OAUTH_LINK_SECRET}
    link-ttl-seconds: ${OAUTH_LINK_TTL_SECONDS:900} # 5분
    link-cookie-name: "oauth-link-token-dartoo"
    issuer: "Dartoo"
  security:
    worker-api-key: ${WORKER_API_KEY}
  terms:
    tos-version: ${TERMS_TOS_VERSION:1.0.0}
    privacy-version: ${TERMS_PRIVACY_VERSION:1.0.0}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000} # CORS 허용 URL을 설정

revenuecat:
  webhook:
    secret: ${REVENUECAT_WEBHOOK_SECRET}
  api:
    key: ${REVENUECAT_API_KEY}

mail:
  from: ${MAIL_FROM}
  verification:
    activation-expire-minutes: ${MAIL_ACTIVATION_EXPIRE_MINUTES}
    password-reset-expire-minutes: ${MAIL_PASSWORD_RESET_EXPIRE_MINUTES}
  base-url: ${MAIL_BASE_URL}

logging:
  level:
    org.springframework.security: ${SECURITY_LOG_LEVEL}
    org.springframework.web: ${APP_LOG_LEVEL}
    dartoo.accountService: ${APP_LOG_LEVEL}

kind: Application